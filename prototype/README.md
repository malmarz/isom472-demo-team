# Prototype

The Design Lead's HTML prototype and the screen list live here.

It is built in Gemini Enterprise and committed to this repository — not left in the browser.

It is opened in a browser: these are files you double-click, not something that needs a
build step or a server.
